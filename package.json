{
    "name": "smart-budget-buddy-frontend",
    "version": "1.0.0",
    "description": "Frontend for Smart Budget Buddy application",
    "main": "js/app.js",
    "scripts": {
        "backend": "pip install -r requirements.txt && python -m uvicorn main:app --reload --host localhost --port 8000",
        "dev": "python -m http.server 3000",
        "build": "echo 'Building frontend assets...' && mkdir -p dist && cp -r * dist/",
        "test": "echo 'No tests specified yet'"
    },
    "keywords": [
        "budget",
        "finance",
        "ai",
        "chatgpt"
    ],
    "author": "Your Name",
    "license": "MIT",
    "devDependencies": {
        "live-server": "^1.2.2"
    }
}